namesurfer note since this demo is running as an applet and not a standalone program the display will not resize if the browser window is resized the browser window is not the program window this applet is only meant to demonstrate the basic workings of the namesurfer assignment without the resizing component
