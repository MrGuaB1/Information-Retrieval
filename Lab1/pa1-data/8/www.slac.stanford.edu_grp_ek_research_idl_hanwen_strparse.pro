name strparse purpose this function parses a string using the space token category string processing calling sequence result strparse string inputs string the string to be parsed example create a string variable s s this is a string with in it now parse s by entering ss strparse s help ss print transpose ss idl prints ss string array 8 this is a string with in it modification history written by han wen may 1996 function strparse str1 inf 100000 strs str strtrim str1 2 pspc strpos str if pspc eq 1 then return str repeat begin s strmid str 0 pspc strs strs s str strtrim strmid str pspc 1 inf 1 pspc strpos str endrep until pspc eq 1 if str ne then strs strs str return strs 1 end
