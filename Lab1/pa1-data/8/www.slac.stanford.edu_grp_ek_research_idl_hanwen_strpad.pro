name strpad purpose this function returns the source string padded with leading and or trailing white space characters category strlib calling sequence result strpad source length pos inputs source a string or number you want padded with white space characters length the total length of the returned padded string optional inputs pos position of the source string within the returned padded string 0 default outputs the source parameter is returned as a string with leading and or trailing white space characters restrictions the length and pos parameters must be in the range 0 255 example let's say you want bob to have a length of 10 characters with spaces padded after bob bob10 strpad bob 10 or if you want bob to be at the end bobend strpad bob 10 7 modification history written by han wen december 1994 function strpad source length pos np n_params if np lt 2 or np gt 3 then message must be called with 2 3 parameters source length pos if length gt 255 then message length must be 0 255 sz size source if sz 0 ne 0 then message source parameter must be a scalar if sz 1 eq 7 then ssrc string source else ssrc strtrim source 2 remove leading trailing blanks from the number slen strlen ssrc sdiff length slen blank case np of 2 begin if sdiff gt 0 then begin fmt a strtrim sdiff 2 return ssrc string blank format fmt endif else begin fmt a strtrim length 2 return string ssrc format fmt endelse end 3 begin ldiff pos rdiff length ldiff slen strtmp ssrc if ldiff gt 0 then begin fmt a strtrim ldiff 2 strtmp string blank format fmt strtmp endif if rdiff gt 0 then begin fmt a strtrim rdiff 2 strtmp strtmp string blank format fmt endif fmt a strtrim length 2 return string strtmp format fmt end endcase end
